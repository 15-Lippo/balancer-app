import { Provider, EnsProvider } from '@ethersproject/providers';

declare const _resolveAvatar: (provider: Provider & EnsProvider, ensName: string, address: string) => Promise<string | null>;
/**
 * @notice Resolves the avatar attached to a given ENS name
 * @param provider A provider with ENS resolution capabilities
 * @param addressOrEns The ENS name (or address) to resolve the avatar of.
 * @returns a URL to the provided ENS name's avatar or null if no avatar exists
 */
declare const resolveENSAvatar: (provider: Provider & EnsProvider, addressOrEns: string) => Promise<string | null>;

export { _resolveAvatar, resolveENSAvatar };
